# Recursive Chaos Tumblr Bot #

Create a properties.ini in the /properties directory to provide your credentials. See properties-example.ini.

## To run ##

`npm install`

`npm start`

## To use ##

`curl localhost:3000\random`